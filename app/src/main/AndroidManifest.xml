<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daffatahta.go2workapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.formLowongan"></activity>
        <activity android:name=".activity.Keahlian" />
        <activity android:name=".activity.userRegister" />
        <activity android:name=".activity.companyRegister" />
        <activity android:name=".activity.companyProfile" />
        <activity android:name=".activity.userProfile" />
        <activity android:name=".activity.login" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.register" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>